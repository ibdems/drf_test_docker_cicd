services:
  db:
    image: postgres:15
    ports:
      - 5459:5432
    environnement:
      - POSTGRES_USER=task
      - POSTGRES_PASSWORD=task
    volumes:
      - pg_data_task:/var/lib/postgresql/data

  volumes:
    pg_data_task:
